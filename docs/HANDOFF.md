# 交接文档（最后更新 2026-08-18）

> **项目状态：开发暂停，等公司安排上架。**
> 功能和合规都做到了「可以提交应用商店」的程度，代码全部已部署上线。
> 作者去做别的项目了，不再持续开发——所以这份文档不是「明天接着干什么」，
> 而是「现在是什么状态 + 真要上架时照着做什么」。

## 🔴 上架前必做清单

按顺序，前两条卡在公司资质上，其余是代码/配置动作。

| # | 事项 | 谁做 | 卡在哪 |
|---|---|---|---|
| 1 | `backend/src/handlers/legal.ts` 的 `ENTITY` 常量换成企业名义，正文里"个人开发者"措辞一并改，然后 `npm run deploy` | 开发 | 等企业资质 |
| 2 | App 备案（工信部）、安卓软著登记 | 你自己走官方渠道 | 等企业资质。软著材料已备好，见桌面「人生清单-软著申请材料」 |
| 3 | **管理端「公告与版本 → 功能开关 → 对用户显示排行榜」关掉**，过审后再打开 | 开发/运营 | 随时可做。理由见下 |
| 4 | **管理端「公告与版本 → 功能开关 → 数据留存清理」打开** | 开发/运营 | 随时可做。理由见下 |
| 5 | 商店后台填「账号删除网址」：<https://renshengqingdan-d8feva5q55d12bab-1258070735.tcloudbaseapp.com/account-deletion/> | 你 | 随时 |
| 6 | 提审前重跑 `python3 scripts/seed_review_account.py`，把测试账号 `stabtest02` / `test123456` 的任务日期刷到当天 | 你 | 提审当天 |

**第 3 条为什么**：库里有 100 个 `demo_seed` 演示用户混在真实排行榜里，
榜前几名全是假的、用户分不出来——审核可能认定「虚假繁荣」。关掉后心愿页右上角
排行榜入口和心愿详情「N 人也想做」一起消失（都是这批数据算出来的），过审再打开，
**不用发版**。

**第 4 条为什么**：隐私政策第七节已经对用户公示了具体保留期（登录日志 90 天、
崩溃 180 天、注销用户 30 天物理删除等），但清理任务的总开关**目前是关着的**
（2026-08-18 上线时先关掉观察）。App 真上线有真实用户后还关着，
就等于「写了没做」。功能和测试都齐了，打开即可。

## 项目现状

三端都已部署上线，随时可用：

| | 地址 / 状态 |
|---|---|
| 后端 API | CloudBase 云函数，`cd backend && npm run deploy` 部署 |
| 管理端 | <https://renshengqingdan-d8feva5q55d12bab-1258070735.tcloudbaseapp.com> ，`cd admin && npm run deploy` |
| App | Flutter，push 到 main 自动打包发 GitHub Releases |
| 内测下载页 | <https://.../api/releases>（上架后即废弃） |
| 网页注销页 | <https://.../account-deletion/> |

**测试**：后端 `cd backend && npm test`（18 个，node:test，零额外依赖）；
前端 `cd frontend && flutter test`（99 个）。两边都应该全绿。

### 做完了的（别重做）

- **管理端三阶段**：后端管理 API / 管理端前端 / App 端配套，全部完成并上线。
  设计稿 `docs/superpowers/specs/2026-08-11-admin-console-design.md`，
  计划文件在 `docs/superpowers/plans/` 下
- **专业化路线 6 项里做完 5 项**：管理端、崩溃监控（自研）、本地通知、稳定性测试、
  合规上架（除了等资质的部分）。**只有第 4 项「增量拉取+缓存恢复」没做**——
  那是新功能，作者决定收尾时不做了；现在的「云端全量拉取」能正常工作，只是没有离线缓存
- **发版流水线**：Gradle 8.14 / AGP 8.11.1 / Kotlin 2.2.20，CI 锁 Flutter 3.47.0。
  ⚠️ 想升 Flutter 必须走「本地拿目标版本打包 + 装模拟器跑通」再改 CI，
  不能直接改 yml 里的版本号——2026-08-17 因为不锁版本静默挂了 10 次构建、5 天没人发现
- **合规**：隐私政策/用户协议、首启同意弹窗、同意前不联网采集、权限场景说明、
  第三方 SDK 清单、排行榜隐身开关、排行榜远程开关、网页注销、数据留存清理

### 明确决定不做的（别再研究）

- **增量拉取 + 缓存恢复**：见上，收尾时决定不做
- **CI 传 APK 到云存储**：要配 `TCB_SECRET_ID`/`TCB_SECRET_KEY` 两个仓库 secret。
  下载页只是上架前的临时通道，上架后就废弃，不值得为它配密钥
- **崩溃符号化**：拿到的是未符号化地址堆栈，看得出崩在哪个库/多少次/什么机型，
  读不出具体哪行。要符号化得按版本归档 dSYM + 服务端跑 atos，CloudBase 云函数跑不了
  原生工具。真有高频崩溃时手工对那一个版本符号化即可
- **接第三方崩溃监控**：Bugly 专业版起步 3.2 万/年、QQ 版无官方 Flutter SDK；
  sentry.io 国内访问受限且数据出境影响备案。将来真要上推荐自托管 GlitchTip

### 已知限制（不是 bug，是当前设计的边界）

- **管理端统计是「拉全量到内存里算」**。`all*` 六个方法已经改成翻页拉全量
  （2026-08-18，之前是单次 limit(1000) 静默截断，实测线上 1636 条 wishes 只返回
  1000 条），但翻页上限是 20 页 = 2 万条，撞到会打日志。真到那个量级，
  这套「拉全量再内存聚合」的做法本身就该换成数据库侧聚合了。
- `/admin/quota` 的 limit 字段 API 不返回（恒 null），前端按"只显示用量"处理。
- 演示用户编辑是**全量回填**语义（非增量 patch），前端表单必须回填全部字段。
- `announcements` 的 startAt/endAt 是毫秒时间戳；该集合里 `sys_min_version`
  和 `sys_features` 两个特殊文档是配置不是公告，各处列表都要滤掉。
- `/config` 挂在用户 JWT 路由下——**未登录拿不到**公告/minVersion/功能开关。
  排行榜入口因此额外加了「已登录」判断（没登录本来也上不了榜）。
- 崩溃角标只在有人登后台时才起作用，没有主动推送。

### ⚠️ 换电脑必带的 gitignored 文件（含密钥，不进 git，自己安全拷贝）

- `backend/cloudbaserc.json` —— **没有它绝对不要在新电脑上执行 `npm run deploy`**（会用空密钥覆盖线上配置）。里面 7 个 envVariables：
  - **JWT_SECRET** —— 换了会把全体用户登出！2026-08-11 已踩过一次
  - **ADMIN_KEY** —— 管理端登录密钥
  - **TMAP_KEY** —— 腾讯位置服务（管理端选点地图 + 逆地理编码）
  - **TC_SECRET_ID / TC_SECRET_KEY** —— 腾讯云 API（额度监控）
  - MODE / CLOUDBASE_ENV_ID —— 非密钥，丢了也能照着仓库补
  拷过去之后 `python3 -c "import json;print(list(json.load(open('backend/cloudbaserc.json'))['functions'][0]['envVariables']))"` 核对一下有没有少。
- 可选：`.claude/settings.local.json`（权限偏好 bypassPermissions，重建也行）。

### 网页版注销页（2026-08-17/18 做完）

源码 `delete/index.html`（静态页，不进构建）+ 后端 `POST /api/account-deletion` + 管理端「注销申请」页。
政策对照见 `delete/COMPLIANCE.md`（里面有 Apple/Google 原文逐条核对结论，别重新研究一遍）。

- 线上：<https://renshengqingdan-d8feva5q55d12bab-1258070735.tcloudbaseapp.com/account-deletion/>
  —— **这个地址要填进 Play Console 数据安全表单和 App Store Connect**
- 部署：`tcb hosting deploy delete account-deletion -e renshengqingdan-d8feva5q55d12bab`
  ⚠️ 别直接传 `delete/` 目录，`COMPLIANCE.md` 会被一起推成公网可访问的；先拷 index.html + logo.png 到临时目录再传
- 流程：网页填账号 + 密码 → 校验通过登记进 `deletion_requests` → **管理端「注销申请」页人工点「执行注销」**
  （承诺 30 天内完成）。密码校验不能拆：账号没绑邮箱手机，事后无法核实申请人是不是号主
- **申请堆着不处理就是违反 Google Play 的删除要求**，管理端侧边栏有未处理角标

### 数据留存清理（2026-08-18 做完）

`backend/src/handlers/cleanup.ts` + 每天 04:00 的定时触发器。**这不是可选的运维脚本**：
隐私政策第七节把保留期写成了具体天数，这个任务就是那段承诺的兑现物；
《个人信息保护法》第十七/十九条也要求告知保存期限并且不超过必要的最短时间。

| 数据 | 保留期 | 判定字段 |
|---|---|---|
| 登录日志 logins | 90 天 | `at` |
| 行为事件 events | 90 天 | `at` |
| 崩溃 crashes | 末次发生后 180 天 | `lastAt`（不是 firstAt——还在复发的 bug 不该被清） |
| 意见反馈 feedback | 处理完成后 1 年 | `handled==true && createdAt`，**未处理的永不清** |
| 注销用户 | 30 天后物理删除 | `users.deletedAt` |

- **改天数要同步改隐私政策第七节的表格**，两边必须一致，否则又变成说了没做
- 注销现在是「立即失效 + 30 天后物理删」：`softDeleteUser` 打标记时顺手盖
  `deletedAt`，清理任务据此判断到期。到期时 `hardDeleteUser` 会连带清掉该用户的
  wishes/tasks/letters/logins/events/feedback/deletion_requests，崩溃记录只清 account
  归属字段（那条指纹下还聚合着别人的崩溃次数，整条删会丢别人的数据）
- 每次运行每类最多 300 条、用户最多 20 个——云函数超时只有 20 秒，宁可分几天删完。
  实测跑一次 2.4 秒
- 🔴 **总开关默认关，目前是关着的**（2026-08-18 上线时先关掉观察）。
  管理端「公告与版本 → 功能开关 → 数据留存清理」可开。开关存在 `sys_features`
  文档的 `cleanupEnabled` 字段
  - **兜底方向跟 showRank 相反**：showRank 读不到配置按「开」，清理读不到按「关」。
    这是有意的——会物理删数据的自动任务，配置异常时宁可漏删不可错删
  - ⚠️ **长期关着 = 隐私政策第七节的承诺没兑现**，而且 logins/events 会一直涨到
    把管理端首页统计撑失真。观察一阵没问题就该打开
- 手动跑：`POST /admin/cleanup`（带 X-Admin-Key）；开关关着时手动也不跑，
  传 `{"force":true}` 可以强制跑一次（验证用）。定时器：
  `tcb fn trigger create api --trigger-name dailyCleanup --cron "0 0 4 * * * *"`
  ⚠️ 触发器配置在云端，不在仓库里（cloudbaserc.json 是 gitignored）——
  **换环境/重建函数后要记得重新创建这个触发器**，否则清理会静默停止
- 实测验证过完整链路：造账号→产生心愿和登录日志→注销→把 deletedAt 改到 31 天前→
  跑清理→用户记录和全部关联数据确认物理消失
- 首次运行给 78 个早期注销的测试账号补盖了 `deletedAt`（那个字段是这次才加的，
  老数据没有，不能当成"很久以前删的"直接清掉，所以补成当下重新计时）
- **顺带解决了一个性能隐患**：`allLogins`/`allEvents` 是拉全量没分页的，
  破千条后管理端首页统计会静默失真（见上面「计划 A 遗留事项」第一条）。
  90 天保留期把这两张表的规模钉住了

### 管理端待办角标

侧边栏「注销申请」和「崩溃」两项都有未处理数角标（`admin/src/App.tsx` 的
`usePendingCounts` + `withBadge`），处理完自动消失。崩溃页默认只看未处理，
行内有「标记处理」按钮。

同指纹再次崩溃**不会**把 `resolved` 重置回未处理——已经判定「不修了」的问题
不该因为又崩一次就重新冒出来。

⚠️ 角标只在「有人登后台」时才起作用。真要无人值守时知道线上崩了，
得做邮件/微信推送，那是另一件事，没做。

### 杂项备忘

- **腾讯位置服务没做实名认证，管理端选点的「地点搜索」用不了**（不阻塞上架，手填经纬度可替代）。 管理端选点地图 2026-08-15 从高德换成腾讯地图，`TMAP_KEY` 已申请并配好部署（key 在 cloudbaserc.json，gitignored）。当天实测：
  - 逆地理编码（`/ws/geocoder/v1/`）正常——点地图选点、自动回填省份都能用；
  - **地点搜索（`/ws/place/v1/search`）返回 `status:121「此key每日调用量已达到上限」**——未实名认证的个人开发者这个 API 免费额度极少，几次调用就用完。**解决办法：去 lbs.qq.com 控制台做实名认证提额**（额度每天重置，会自动恢复一点点，但不认证始终不够用）。
  - 交互差异：腾讯 `MultiMarker` 官方文档没有拖拽标记能力（高德 `Marker` 有 `draggable`），所以选点改成「点地图放标记」+ 手填经纬度微调，不做拖拽。
  - 没配 key 时 `mapConfig` 返回 `available:false`，管理端退化成手填经纬度，不影响其它功能。
- 用户手机/模拟器因 2026-08-11 JWT 密钥轮换需重新登录一次。
- 测试账号 `stabtest02` / `test123456`（稳定性测试时注册的，后来补成了提审账号）。
  云端现有：51 条心愿（12 条已实现）、9 条任务（跨前后几天、有完成有待办、都挂了心愿所以
  日历上有颜色）、2 封时光胶囊（1 封已到期可拆、1 封锁到十年后）、6 处地图打卡、3 枚勋章、
  头像/性别/生日都填了。**这些是补出来的，不是自然攒的**——最初这账号只有心愿，任务/胶囊/
  打卡全是 0，审核员登进去会以为那几个功能不存在，这是「无法评估功能」类驳回的常见成因。
  补充用 `scripts/seed_review_account.py`，固定 _id 所以重跑是覆盖不是新增；任务日期按
  运行当天相对算，**隔久了要重跑**，否则日历上只剩历史、今天空着。
- ~~`/api/releases` 下载页没有兜底~~ ✅ 2026-08-18：版本列表快照存进 `sys_cache/releases`
  文档，GitHub 不通时用快照渲染并在页面顶部挂黄条说明。存的是原始列表不是渲染好的 HTML
  ——云存储链接是临时签名的（几小时过期），每次渲染现换一批，否则退化状态下按钮是死链。
  实测方式：临时把 `REPO` 改成不存在的仓库重新部署，确认页面仍 200 且列表完整。
- CI 从没把 APK 传上云存储（`tcb storage list releases` 是空的）：
  `.github/workflows/release.yml` 的上传步骤要 `TCB_SECRET_ID`/`TCB_SECRET_KEY`
  两个仓库 secret，没配就直接 skip，所以下载按钮永远指向 GitHub 镜像。
  **不打算修**（2026-08-18 决定）：这个下载页只是上架前给合伙人内测用的临时通道，
  上了应用商店就不用了，不值得为它配密钥。页面兜底已经做了，GitHub 抽风时
  至少能打开、能看到列表，够用。别再花时间研究这条。

## 图片资源体系（2026-08-15/16 做完，路线外插入项）

App 里所有展示类图片都能在管理端换了，不用发版。三层结构：

1. **打包兜底**（`frontend/assets/`）—— 保证没网/未登录/首次启动/云挂了都有东西显示，**别为了省体积删掉**
2. **云端下发**（`/config`）—— 运营改图的入口，优先级高于内置图
3. **磁盘缓存**（`frontend/lib/photos.dart` 的 `cachedImageFile`）—— 命中直接读本地文件，不发网络

### 管理端「图片素材」6 张表

| 表 | 用途 | 条数 |
|---|---|---|
| poster_task / poster_wish | 任务/心愿成绩单海报 | 5 / 5 |
| poster_done | 心愿达成弹窗底图 | 4 |
| hero_images | 心愿没照片时的兜底头图 | 5 |
| cover_declare | 宣告卡封面（可左右滑） | 3 |
| cover_done | 凭证卡封面 | 4 |

云存储：`content/posters/`(14) `content/hero/`(9) `content/honor/`(14)。
种子脚本 `backend/scripts/seed-content.ts` + `seed-poster-images.ts` 都已同步，重新播种不会丢。

### 几个容易踩的点（都已踩过并修掉）

- **别把上传路径改成「按用途固定文件名然后覆盖」**。App 按去掉签名的稳定链接做磁盘缓存，
  覆盖同名文件的话 URL 不变 → 缓存永远命中旧图，用户得重装 App。现在 `admin/upload.ts`
  每次生成随机路径是**有意为之**，那里留了警告注释。
- **云存储链接直接 `Image.network` 取不到**（403），必须走 `/photo-urls` 换临时链接，
  统一用 `WishPhoto` 组件，它已经封装好了换链接 + 失败回退。
- **临时链接每次签名都不同**，所以缓存 key 必须用去掉 `?` 后的稳定链接，否则内存/磁盘
  缓存全都命中不了，表现就是每次进页面闪一下。
- **图片分配用 `doneNumberOf(w)` 轮流**，不是 id 哈希取模——哈希不保证均匀，实测 11 条
  分 4 张是 4/4/2/1，用户观感就是「没换」。
- `/config` 在**启动、登录、切回前台**（5 分钟节流）三个时机拉。运营改完图，用户切个
  后台再回来就生效。

## 专业化路线（2026-08-12 评估的执行顺序，现已收尾——留作背景参考）

1. ~~管理端收尾（计划 B Task 2-8 → 计划 C）~~ ✅ 2026-08-12 完成
2. ~~崩溃监控~~ ✅ 2026-08-15 完成（**自研，没接第三方**）。实现见 `frontend/lib/crash_reporter.dart`、`backend/src/handlers/crash.ts`、管理端「崩溃」页。
   - **架构**：崩溃当下**只写本地不发网络**（进程可能马上没了、用户可能没网），下次启动补发，发成功才清。后端按指纹（异常类型 + 堆栈前几帧，剔除地址/数字）聚合，同一 bug 崩一万次归成一条只累加 count。
   - **三层覆盖**：① Dart 层未捕获异常（两端都有，完整可读堆栈）② 原生崩溃（**只有 iOS**，KSCrash 2.6.0）③ 异常退出计数（启动写 alive 标记、正常退后台清除，两端都有）。
   - ⚠️ **Android 的 xCrash 已摘除，别再加回来**：实测它的 `libxcrash.so` LOAD 段 `align=0x1000`（4KB），过不了 Google Play「targetSdk>=35 必须支持 16KB 内存页」的要求（2025-11-01 起，延期至 2026-05-31）——上架硬阻塞，16KB 页真机也加载不了。根因是它停更在 NDK r28 之前。**当时用 4KB 页模拟器测试全过，是模拟器骗人的典型案例**；这类 native 库合规问题必须直接查构建产物（`llvm-readelf -l xxx.so` 看 LOAD align）。
   - 替代品都查过没有更好的：Tencent/matrix 停更在 2024-07 更旧；sentry-java 虽活跃但 native 崩溃产出 minidump，必须配 Sentry symbolicator 服务端才能读。
   - **未做符号化**：拿到的是未符号化地址堆栈，看得出崩在哪个库/多少次/什么机型版本，读不出具体哪一行。要那个得按版本归档 dSYM 并在服务端跑 atos——CloudBase 云函数跑不了原生工具，需单独机器。真有高频崩溃要定位时，手工对那一个版本符号化即可，不必建全自动流水线。
   - 曾评估过的第三方（结论仍有效，别重复评估）：**Bugly 专业版收费**（无免费额度，起步 3.2 万/年）；Bugly QQ 版免费但无官方 Flutter SDK；**sentry.io 国内访问受限**且数据出境影响备案上架。将来真要上第三方，推荐自托管 GlitchTip（Sentry API 兼容，可复用官方 SDK 只改 DSN）。
3. ~~本地通知做成真功能：flutter_local_notifications，场景=任务到期提醒 + 时光胶囊开启日 + 心愿期限 + 沉睡唤回（留存命脉）~~ ✅ 2026-08-15 完成
4. 增量拉取+缓存恢复 —— ❌ **决定不做**（2026-08-18 收尾时定的，见上面「明确决定不做的」）
5. ~~一轮系统性稳定测试：弱网 / 杀进程恢复 / 低端机 / 平板~~ ✅ 2026-08-15 完成（iOS+Android 模拟器实测：断网时同步失败横幅正常亮起、恢复后重试成功；编辑数据中途强杀重启数据不丢不错乱；768MB 单核低配模拟器跑通 51 条长列表无崩溃无 ANR；iPad 无溢出报错，顺带发现并修复了平板卡片布局问题）
6. 合规与上架（流程周期长，尽早启动并行走）：隐私政策+用户协议页 → App 备案 → 安卓软著 → 上架华为/小米/App Store
   - ~~隐私政策+用户协议页~~ ✅ 2026-08-17 完成。`backend/src/handlers/legal.ts`，公开路由 `/privacy`
     `/terms`（仿 releases.ts 的静态 HTML 返回模式）。内容按代码里实际的数据处理逻辑逐项写的，
     不是套模板；专门提了「公开主页会显示生日算出的年龄」这条容易被忽略的隐私点。
     App 端：注册表单加了勾选提示+链接，「我的」设置页常驻两个入口。
     用 app-submission-check 技能的 45 项清单核对过：E-27（性别/生日不强制填）、E-28（隐私链接
     可访问）、G-35（账号注销）现在都能过；G-36 测试账号已有（`stabtest02`/`test123456`）。
   - ⚠️ **运营主体现在是占位**：`legal.ts` 顶部 `ENTITY` 常量写的是"个人开发者，企业主体申请中"。
     企业资质批下来之后，把这个常量和正文里"个人开发者"的措辞一起换成企业名义，
     这两个页面公网可访问，App 备案和应用商店提交时都要用这两个链接，主体信息必须跟实名/
     备案主体一致，批下来那天记得第一时间改，别用占位内容去交材料。
   - ~~国内审核合规四件套~~ ✅ 2026-08-17 完成：首启隐私同意弹窗（不同意退回本地
     预览，不闪退）、同意前不联网采集（崩溃上报改成本地落盘+同意后补发）、
     权限场景说明（定位/相册/相机三处，系统弹窗前先说明用途）、隐私政策补
     「同意流程」和「第三方 SDK 目录」（KSCrash 是全项目唯一第三方数据组件，仅 iOS）。
   - ~~排行榜隐私~~ ✅ 2026-08-17：加了「排行榜隐身」开关（用户自选不上榜，
     users.hideFromRank，榜单/名次/热度穿透四处查询统一过滤）。
   - 🔴 **上架送审前必做：把管理端「公告与版本 → 功能开关 → 对用户显示排行榜」关掉。**
     库里 100 个 demo_seed 演示用户混在真实榜单里展示，审核时可能被认定虚假繁荣。
     关掉后心愿页右上角排行榜入口 + 心愿详情「N 人也想做」一起消失（都是这批数据
     算出来的），**过审后再打开，不用发版**。开关落在 announcements 集合的
     sys_features 文档，App 端读 /config 的 features.showRank。
   - **App 备案、安卓软著两项本身需要用户自己去官方渠道申请**（备案要企业资质、软著要走版权
     登记流程），不是能直接在代码里做完的事——等企业资质批下来后，备案填报时的 App 信息
     （名称/版本/隐私政策链接/功能截图）这边随时能配合出。
